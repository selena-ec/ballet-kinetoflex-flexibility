{
  "name": "kineto",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "node --test",
    "check": "npm run format:check && npm run lint && npm test"
  },
  "devDependencies": {
    "eslint": "^10.0.0",
    "prettier": "^3.6.2",
    "vite": "^7.1.5"
  }
}
